{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}
{% block jumboTitle %}Edit Events{% endblock %}
{% block jumboLeadtext %} {{ event.name }} {% endblock %}

{% block body %}
    <div class="row">
        <div class="form-group col-lg-8">

            {{ include('events/_form.html.twig', {'button_label': 'Update'}) }}

            {# <a class="btn btn-secondary btn-block col-md-4" href="{{ path('events_index') }}">back to list</a> #}

            {# {{ include('events/_delete_form.html.twig') }} #}
        </div>
        <div class="col-lg-3 mt-5 mx-auto">
            <a class="btn btn-outline-secondary btn-block btn-lg rounded-0" href="{{ path('events_index') }}">Back to list</a>
            {{ include('events/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
